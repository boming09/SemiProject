<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM
"http://java.sun.com/dtd/properties.dtd">
<properties>
<!-- paging Count -->
<entry key="getListCount">
   SELECT
          COUNT(*)
     FROM BOOK
</entry>

<entry key="getSearchListCount">
  SELECT
         COUNT(*)
    FROM BOOK
   WHERE BOOK_NAME LIKE '%' || ? || '%' OR AUTHOR LIKE '%' || ? || '%'
</entry>

<entry key="getTitleListCount">
  SELECT
         COUNT(*)
    FROM BOOK
   WHERE BOOK_NAME LIKE '%' || ? || '%'
</entry>

<entry key="getAuthorListCount">
  SELECT
         COUNT(*)
    FROM BOOK
   WHERE AUTHOR LIKE '%' || ? || '%'
</entry>

<!-- book List -->
<entry key="selectList">
	SELECT * 
	     FROM (SELECT ROWNUM RNUM, A.*
		      FROM (SELECT
	                       BOOK_ID
	                     , BOOK_NAME
	                     , CATEGORY_ID
	                     , USER_NAME
	                     , EDITOR
	                     , PUBLICATION_DATE
	                     , PUBLISHER
	                     , PRICE
	                     , STOCK
	                     , SALE_RATE
	                     , SALE_PRICE
	                     , BOOK_INTRO
	                     , BOOK_URL
	                     , FILE_PATH
	                     , AUTHOR_INTRO
	                     , BOOK_IMG
	                     FROM BOOK B
	                     JOIN MEMBER ON(AUTHOR = USER_NO)
	                     ORDER BY PUBLICATION_DATE DESC) A)
	 WHERE RNUM BETWEEN ? AND ?
</entry>

<entry key="selectSearchList">
	SELECT * 
	     FROM (SELECT ROWNUM RNUM, A.*
		      FROM (SELECT
	                       BOOK_ID
	                     , BOOK_NAME
	                     , CATEGORY_ID
	                     , CATEGORY_NAME
	                     , USER_NO
	                     , AUTHOR
	                     , EDITOR
	                     , PUBLICATION_DATE
	                     , PUBLISHER
	                     , PRICE
	                     , STOCK
	                     , SALE_RATE
	                     , SALE_PRICE
	                     , BOOK_INTRO
	                     , BOOK_URL
	                     , FILE_PATH
	                     , AUTHOR_INTRO
	                     , BOOK_IMG
	                     FROM BOOK
	                     JOIN B_CATEGORY USING(CATEGORY_ID)
	                     WHERE BOOK_NAME LIKE '%' || ? || '%' OR AUTHOR LIKE '%' || ? || '%'
	                     ORDER BY PUBLICATION_DATE DESC) A)
	 WHERE RNUM BETWEEN ? AND ?
</entry>

<entry key="selectTitleList">
	SELECT * 
	     FROM (SELECT ROWNUM RNUM, A.*
		      FROM (SELECT
	                       BOOK_ID
	                     , BOOK_NAME
	                     , CATEGORY_ID
	                     , CATEGORY_NAME
	                     , USER_NO
	                     , AUTHOR
	                     , EDITOR
	                     , PUBLICATION_DATE
	                     , PUBLISHER
	                     , PRICE
	                     , STOCK
	                     , SALE_RATE
	                     , SALE_PRICE
	                     , BOOK_INTRO
	                     , BOOK_URL
	                     , FILE_PATH
	                     , AUTHOR_INTRO
	                     , BOOK_IMG
	                     FROM BOOK
	                     JOIN B_CATEGORY USING(CATEGORY_ID)
	                     WHERE BOOK_NAME LIKE '%' || ? || '%'
	                     ORDER BY PUBLICATION_DATE DESC) A)
	 WHERE RNUM BETWEEN ? AND ?
</entry>

<entry key="selectAuthorList">
	SELECT * 
	     FROM (SELECT ROWNUM RNUM, A.*
		      FROM (SELECT
	                       BOOK_ID
	                     , BOOK_NAME
	                     , CATEGORY_ID
	                     , CATEGORY_NAME
	                     , USER_NO
	                     , AUTHOR
	                     , EDITOR
	                     , PUBLICATION_DATE
	                     , PUBLISHER
	                     , PRICE
	                     , STOCK
	                     , SALE_RATE
	                     , SALE_PRICE
	                     , BOOK_INTRO
	                     , BOOK_URL
	                     , FILE_PATH
	                     , AUTHOR_INTRO
	                     , BOOK_IMG
	                     FROM BOOK
	                     JOIN B_CATEGORY USING(CATEGORY_ID)
	                     WHERE AUTHOR LIKE '%' || ? || '%'
	                     ORDER BY PUBLICATION_DATE DESC) A)
	 WHERE RNUM BETWEEN ? AND ?
</entry>

<!-- category List -->
<entry key="categoryList">
SELECT
       CATEGORY_NAME
  FROM BOOK
  JOIN B_CATEGORY USING(CATEGORY_ID)
 GROUP BY CATEGORY_NAME
</entry>

<entry key="searchCategoryList">
SELECT
       CATEGORY_NAME
  FROM BOOK
  JOIN B_CATEGORY USING(CATEGORY_ID)
 WHERE BOOK_NAME LIKE '%' || ? || '%' OR AUTHOR LIKE '%' || ? || '%'
 GROUP BY CATEGORY_NAME
</entry>

<entry key="titleCategoryList">
SELECT
       CATEGORY_NAME
  FROM BOOK
  JOIN B_CATEGORY USING(CATEGORY_ID)
 WHERE BOOK_NAME LIKE '%' || ? || '%'
 GROUP BY CATEGORY_NAME
</entry>

<entry key="authorCategoryList">
SELECT
       CATEGORY_NAME
  FROM BOOK
  JOIN B_CATEGORY USING(CATEGORY_ID)
 WHERE AUTHOR LIKE '%' || ? || '%'
 GROUP BY CATEGORY_NAME
</entry>

<!-- select Book -->
<entry key="selectBook">
SELECT
       BOOK_ID
     , BOOK_NAME
     , CATEGORY_ID
     , CATEGORY_NAME
     , USER_NO
     , AUTHOR
     , EDITOR
     , PUBLICATION_DATE
     , PUBLISHER
     , PRICE
     , STOCK
     , SALE_RATE
     , SALE_PRICE
     , BOOK_INTRO
     , BOOK_URL
     , FILE_PATH
     , AUTHOR_INTRO
     , BOOK_IMG
     FROM BOOK
     JOIN B_CATEGORY USING(CATEGORY_ID)
    WHERE BOOK_ID = ?
</entry>

</properties>
